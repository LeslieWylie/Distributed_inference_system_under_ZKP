//! v3-circuit — R1CS circuit crate for MNIST slice FCircuits.
//!
//! Phase 1 only reserves the crate skeleton; the actual `FCircuit`
//! implementations live in Phase 2.  See `docs/refactor/v3/` for the
//! full specification.

/// Phase 1 placeholder; Phase 2 will replace this with real APIs.
pub fn dummy() {}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn dummy_is_a_noop() {
        dummy();
    }
}
