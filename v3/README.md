# v3 — Collaborative Nova-IVC (active development line)

See `docs/refactor/v3/` for the full design spec. This tree is the new main line; `v2/` remains as a frozen baseline for comparison experiments.

Phase 1 will populate the Rust workspace and Python bridge. As of Phase 0 only the directory skeleton exists.
